{
  "routes": [
    {
      "navigationFallback": {
        "rewrite": "EmployeeList",
        "exclude": [
          "/images/*.{png,jpg,gif,ico}",
          "/*.{css,scss,js}"
        ]
      }
    }
  ]
}